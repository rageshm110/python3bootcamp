
# tuples are similar to lists but they are immutable
# syntax
## (value1, valu2, value3)
import os

os.system('clear')
tuple_test = (1, 2, 3)
print("{}".format(type(tuple_test)))

tuple_test.index(1) # this will return the index where value is 1 is in the tuple.
